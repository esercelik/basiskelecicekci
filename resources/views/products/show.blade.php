<x-layout title="{{ $product->name }} | {{ $store->name }}" description="{{ $product->short_description }}" image="{{ $product->images->first()?->image_path }}"><section class="mx-auto max-w-6xl px-4 py-12 sm:px-6 lg:px-8"><a href="{{ route('categories.show', $product->category) }}" class="text-sm font-bold text-forest underline decoration-gold underline-offset-4">← {{ $product->category->name }}</a><div class="mt-6 grid gap-10 lg:grid-cols-2 lg:items-start"><div class="overflow-hidden rounded-[2rem] bg-rose-soft"><img src="{{ $product->images->first()?->imageUrl() ?? asset('images/placeholders/buketler.svg') }}" alt="{{ $product->images->first()?->alt_text ?? $product->name }}" class="aspect-square w-full object-cover"></div><div class="lg:pt-6"><p class="text-sm font-bold tracking-[0.18em] text-gold uppercase">{{ $product->category->name }}</p><h1 class="mt-3 font-display text-5xl font-bold leading-tight text-forest">{{ $product->name }}</h1><p class="mt-5 text-lg leading-8 text-ink/75">{{ $product->short_description }}</p><div class="mt-7 flex items-center gap-3"><span class="text-3xl font-bold text-forest">{{ $product->formattedCurrentPrice() }}</span>@if ($product->hasSalePrice())<span class="text-lg text-ink/50 line-through">{{ $product->formattedPrice() }}</span>@endif</div><p @class(['mt-5 inline-flex rounded-full px-3 py-1 text-sm font-bold', 'bg-forest/10 text-forest' => $product->isOrderable(), 'bg-rose-soft text-rose' => ! $product->isOrderable()])>{{ $product->stockLabel() }}</p><div class="mt-7"><x-whats-app-button :product="$product" /></div><div class="mt-9 border-t border-forest/10 pt-7"><h2 class="font-display text-2xl font-bold text-forest">Ürün hakkında</h2><p class="mt-3 leading-7 text-ink/75">{{ $product->description }}</p></div></div></div></section></x-layout>
