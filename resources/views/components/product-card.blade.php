@props(['product'])
@php($displayImage = $product->primaryImage ?? $product->firstImage)
<article class="flex h-full min-w-0 flex-col overflow-hidden rounded-3xl border border-border bg-white shadow-sm shadow-rose/5 transition duration-300 hover:-translate-y-1 hover:border-forest hover:shadow-xl"><a href="{{ route('products.show', $product) }}" class="relative block overflow-hidden bg-rose-soft"><img class="aspect-[4/3] w-full object-cover transition duration-500 hover:scale-105" src="{{ $displayImage?->imageUrl() ?? asset('images/placeholders/buketler.svg') }}" alt="{{ $displayImage?->alt_text ?? $product->name }}"><span @class(['absolute left-4 top-4 rounded-full px-3 py-1 text-xs font-bold', 'bg-forest text-cream' => $product->isOrderable(), 'bg-ink text-white' => ! $product->isOrderable()])>{{ $product->stockLabel() }}</span></a><div class="flex flex-1 flex-col p-5"><a href="{{ route('categories.show', $product->category) }}" class="text-xs font-bold tracking-[0.14em] text-rose uppercase">{{ $product->category->name }}</a><h3 class="mt-2 font-display text-xl font-bold leading-tight text-forest-dark"><a href="{{ route('products.show', $product) }}" class="transition hover:text-rose">{{ $product->name }}</a></h3><p class="mt-2 text-sm leading-6 text-muted">{{ $product->short_description }}</p><div class="mt-5 flex items-end gap-2"><span class="text-lg font-bold text-forest">{{ $product->formattedCurrentPrice() }}</span>@if ($product->hasSalePrice())<span class="pb-0.5 text-sm text-muted line-through">{{ $product->formattedPrice() }}</span>@endif</div><div class="mt-5 flex flex-wrap gap-2"><a href="{{ route('products.show', $product) }}" class="inline-flex min-h-11 items-center justify-center rounded-full border border-forest px-4 text-sm font-bold text-forest transition hover:bg-forest hover:text-cream">İncele</a><x-whats-app-button :product="$product" /></div></div></article>
