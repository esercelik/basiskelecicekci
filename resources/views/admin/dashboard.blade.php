<x-admin.layout title="Genel bakış"><div class="flex flex-wrap justify-end gap-3"><a class="inline-flex min-h-11 items-center rounded-xl bg-forest px-4 text-sm font-bold text-cream" href="{{ route('admin.products.create') }}">+ Ürün ekle</a><a class="inline-flex min-h-11 items-center rounded-xl border border-forest/20 px-4 text-sm font-bold text-forest" href="{{ route('admin.categories.create') }}">+ Kategori ekle</a></div><div class="mt-6 grid gap-4 sm:grid-cols-2 xl:grid-cols-5">@foreach (['Aktif ürünler' => $counts['activeProducts'], 'Pasif ürünler' => $counts['inactiveProducts'], 'Stokta olmayanlar' => $counts['outOfStockProducts'], 'Öne çıkanlar' => $counts['featuredProducts'], 'Aktif kategoriler' => $counts['activeCategories']] as $label => $count)<article class="rounded-2xl bg-white p-5 shadow-sm"><p class="text-sm text-ink/65">{{ $label }}</p><p class="mt-2 text-3xl font-bold text-forest">{{ $count }}</p></article>@endforeach</div><section class="mt-8 rounded-3xl bg-white p-5 shadow-sm sm:p-7"><h2 class="font-display text-2xl font-bold text-forest">Son eklenen ürünler</h2><div class="mt-5 grid gap-3">@forelse ($recentProducts as $product)<a class="flex items-center justify-between gap-4 rounded-xl border border-forest/10 p-3 hover:bg-cream" href="{{ route('admin.products.edit', $product) }}"><span class="min-w-0"><span class="block truncate font-bold text-forest">{{ $product->name }}</span><span class="text-sm text-ink/60">{{ $product->category->name }}</span></span><span class="text-sm font-bold text-forest">{{ $product->formattedCurrentPrice() }}</span></a>@empty<p class="py-8 text-center text-ink/60">Henüz ürün eklenmedi.</p>@endforelse</div></section></x-admin.layout>
