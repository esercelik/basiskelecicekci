<x-admin.layout title="Kategori düzenle"><form class="rounded-3xl bg-white p-5 shadow-sm sm:p-7" method="POST" action="{{ route('admin.categories.update', $category) }}" enctype="multipart/form-data">@method('PUT') @include('admin.categories.form')</form><form class="mt-5" method="POST" action="{{ route('admin.categories.destroy', $category) }}" onsubmit="return confirm('Bu kategoriyi silmek istediğinizden emin misiniz?')">@csrf @method('DELETE')<button class="min-h-11 rounded-xl border border-rose/40 px-4 text-sm font-bold text-rose">Kategoriyi sil</button></form>@error('category')<p class="mt-3 text-sm text-rose">{{ $message }}</p>@enderror</x-admin.layout>
