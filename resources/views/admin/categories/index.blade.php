<x-admin.layout title="Kategoriler"><div class="flex justify-end"><a class="inline-flex min-h-11 items-center rounded-xl bg-forest px-4 text-sm font-bold text-cream" href="{{ route('admin.categories.create') }}">+ Yeni kategori</a></div><div class="mt-6 overflow-hidden rounded-3xl bg-white shadow-sm"><div class="hidden overflow-x-auto md:block"><table class="min-w-full text-left text-sm"><thead class="bg-cream text-forest"><tr><th class="px-5 py-4">Kategori</th><th class="px-5 py-4">Durum</th><th class="px-5 py-4">Sıra</th><th class="px-5 py-4">Ürün</th><th class="px-5 py-4"></th></tr></thead><tbody>@forelse($categories as $category)<tr class="border-t border-forest/10"><td class="px-5 py-4"><div class="flex items-center gap-3"><img class="size-12 rounded-xl object-cover" src="{{ $category->imageUrl() }}" alt=""><span><span class="block font-bold text-forest">{{ $category->name }}</span><span class="text-ink/55">/{{ $category->slug }}</span></span></div></td><td class="px-5 py-4">{{ $category->is_active ? 'Aktif' : 'Pasif' }}</td><td class="px-5 py-4">{{ $category->sort_order }}</td><td class="px-5 py-4">{{ $category->products_count }}</td><td class="px-5 py-4"><a class="font-bold text-forest underline" href="{{ route('admin.categories.edit', $category) }}">Düzenle</a></td></tr>@empty<tr><td class="px-5 py-10 text-center text-ink/60" colspan="5">Henüz kategori yok.</td></tr>@endforelse</tbody></table></div><div class="grid gap-3 p-4 md:hidden">@forelse($categories as $category)<a class="rounded-2xl border border-forest/10 p-4" href="{{ route('admin.categories.edit', $category) }}"><span class="font-bold text-forest">{{ $category->name }}</span><span class="mt-1 block text-sm text-ink/60">{{ $category->is_active ? 'Aktif' : 'Pasif' }} · {{ $category->products_count }} ürün</span></a>@empty<p class="py-8 text-center text-ink/60">Henüz kategori yok.</p>@endforelse</div></div><div class="mt-5">{{ $categories->links() }}</div></x-admin.layout>
