<x-layout title="{{ $category->name }} | {{ config('store.name') }}" description="{{ $category->description }}" image="{{ $category->image_path }}"><section class="mx-auto max-w-7xl px-4 py-14 sm:px-6 lg:px-8"><a href="{{ route('products.index') }}" class="text-sm font-bold text-forest underline decoration-gold underline-offset-4">← Tüm çiçekler</a><div class="mt-6 grid gap-8 rounded-[2rem] bg-forest p-8 text-cream md:grid-cols-[1fr_300px] md:items-center sm:p-10"><div><p class="text-sm font-bold tracking-[0.18em] text-gold uppercase">Kategori</p><h1 class="mt-2 font-display text-5xl font-bold">{{ $category->name }}</h1><p class="mt-4 max-w-xl leading-7 text-cream/75">{{ $category->description }}</p></div><img src="{{ asset($category->image_path) }}" alt="{{ $category->name }} kategorisi için çiçek illüstrasyonu" class="w-full rounded-3xl"></div><div class="mt-10 grid gap-6 sm:grid-cols-2 lg:grid-cols-3">@forelse ($products as $product)<x-product-card :product="$product" />@empty<div class="col-span-full rounded-3xl bg-white p-10 text-center text-ink/70">Bu kategoride gösterilecek aktif ürün bulunmuyor.</div>@endforelse</div></section></x-layout>
